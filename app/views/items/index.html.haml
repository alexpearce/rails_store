=title 'Items'

%p Browse our, quite frankly, delicious catalog of stuff.

%div.grid
	- @items.each do |item|
		%div.grid_item.twocol.fp_item
			%a{:href => item_path(item), :title => item.name, :rel => 'items_index', :class=> 'meta' }
				%img{:src => item.image.url(:thumb), :alt => item.name, :class => 'imageShadow'}
			%div.bd.pad
				%h3=link_to item.name, item
				:markdown
					#{truncate item.description, :length => 150}
			
	%div.clearfix

- if admin_signed_in?
	%p
		= link_to 'New Item', new_item_path