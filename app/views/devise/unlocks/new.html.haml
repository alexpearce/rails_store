= title 'Resend Unlock Instructions'
= form_for resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post, :class => 'admin' } do |f|
  = devise_error_messages!
  %div.field
    = f.label :email
    = f.text_field :email
  %div.actions
    = f.submit "Send"

= render :partial => "devise/shared/links"
